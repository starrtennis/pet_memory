from django.shortcuts import render

# We need a view is needed to route HTTP traffic from home
# page to appropriate gallery section(s).

# Necesitamos una vista es necesaria para enrutar el tráfico HTTP desde casa
# página a la(s) sección(es) apropiada(s) de la galería.

